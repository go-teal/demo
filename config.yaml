version: '1.0.0'
module: github.com/go-teal/demo
connections:
  - name: default
    type: duckdb
    config:
      path: ./store/db_file.duckdb            
      extensions:
        - postgres
        - httpfs         
      # extraParams: 
      #   - name: "name"
      #     value: "value"